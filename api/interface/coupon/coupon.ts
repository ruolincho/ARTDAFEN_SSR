export namespace ICoupon {

    export interface Row {
        discountRate: string
        expire: string
        unit: string,
        startTime: number
        endTime: number
        link: string
        code: string
    }
}
